# Funder Logo Library

Upload candidate funder, programme and support logos here using lowercase, hyphen-separated semantic filenames.

Move a logo to `../selected/` only after public-use permission is confirmed and the live website references it. Do not place timestamped exports or unapproved partner logos in `selected/`.
